 # add a select box to the side bar
    # st.markdown("<h3 style='text-align: center; color: white;'>Select a Dataset</h3>", unsafe_allow_html=True)
    # dataset = st.selectbox("", ["Iris", "Wine", "Breast Cancer", "Boston Housing", "Diabetes", "Digits", "Linnerud", "Wine Quality"])
    # st.markdown("<h3 style='text-align: center; color: white;'>Select a Model</h3>", unsafe_allow_html=True)
    # model = st.selectbox("", ["Linear Regression", "Logistic Regression", "KNN", "SVM", "Decision Tree", "Random Forest", "K-Means", "PCA", "LDA", "Neural Network"])
    # st.markdown("<h3 style='text-align: center; color: white;'>Select a Task</h3>", unsafe_allow_html=True)
    # task = st.selectbox("", ["Classification", "Regression", "Clustering", "Dimensionality Reduction"])
    # st.markdown("<h3 style='text-align: center; color: white;'>Select a Metric</h3>", unsafe_allow_html=True)
    # metric = st.selectbox("", ["Accuracy", "Precision", "Recall", "F1 Score", "R2 Score", "Mean Squared Error", "Mean Absolute Error", "Explained Variance Score", "Adjusted Rand Index", "Silhouette Coefficient"])
    # st.markdown("<h3 style='text-align: center; color: white;'>Select a Split</h3>", unsafe_allow_html=True)
    # split = st.selectbox("", ["Train/Test Split", "Cross Validation"])
    # st.markdown("<h3 style='text-align: center; color: white;'>Select a Visualization</h3>", unsafe_allow_html=True)
    # viz = st.selectbox("", ["Scatter Plot", "Histogram", "Heatmap", "Box Plot", "Correlation Matrix", "Parallel Coordinates", "Andrews Curves", "RadViz", "Pair Plot", "Profile Report"])
    # st.markdown("<h3 style='text-align: center; color: white;'>Select a Feature Selection Method</h3>", unsafe_allow_html=True)
    # feature_selection = st.selectbox("", ["None", "Select K Best", "Recursive Feature Elimination", "Principal Component Analysis", "Linear Discriminant Analysis"])
    # st.markdown("<h3 style='text-align: center; color: white;'>Select a Hyperparameter Tuning Method</h3>", unsafe_allow_html=True)
    # hyperparameter_tuning = st.selectbox("", ["None", "Grid Search", "Random Search", "Bayesian Optimization", "Genetic Algorithm"])
    # st.markdown("<h3 style='text-align: center; color: white;'>Select a Model Explainability Method</h3>", unsafe_allow_html=True)
    # model_explainability = st.selectbox("", ["None", "Permutation Importance", "Partial Dependence Plots", "SHAP Values", "LIME", "ELI5"])